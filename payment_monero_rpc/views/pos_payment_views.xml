<odoo>
    <record id="view_pos_payment_method_form_inherit" model="ir.ui.view">
        <field name="name">pos.payment.method.form.inherit</field>
        <field name="model">pos.payment.method</field>
        <field name="inherit_id" ref="point_of_sale.pos_payment_method_view_form"/>
        <field name="arch" type="xml">
        	<form string="Payment Methods">
                <group string="Monero">
				    <xpath expr="//field[@name='name']" position="after">
				        <field name="is_monero"/>
						<field name="qr_size" invisible="not is_monero"/>
				    </xpath>
				</group>
			</form>
        </field>
    </record>
</odoo>
